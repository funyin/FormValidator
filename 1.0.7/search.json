{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"FormValidator","text":"<p>Declarative form validation for Jetpack Compose — Android, JVM, and iOS.</p> <p>FormValidator lets you define validation rules alongside your fields, then call <code>validate()</code> once on submit. Errors flow back to each field via callbacks, and the <code>ErrorSafe</code> pattern keeps value, error, and dirty-state in a single <code>remember</code>-able unit.</p> Kotlin GradleGroovy <pre><code>implementation(\"com.github.funyin:FormValidator:LIBRARY_VERSION\")\n</code></pre> <pre><code>implementation 'com.github.funyin:FormValidator:LIBRARY_VERSION'\n</code></pre> <p>Latest version</p> <p>Find the current release on the releases page.</p> <p>At a glance</p> DeclareWrapValidate <pre><code>val validator = FormValidator(\n    flow = FormValidator.Flow.Down,\n    fields = listOf(\n        ValidationField(value = email.value, name = \"Email\", type = FormValidator.Type.Email) {\n            email = email.copy(error = it)\n        }\n    )\n)\n</code></pre> <pre><code>Form(validator) {\n    AppTextField(\n        label = \"Email\",\n        value = email.value,\n        onChange = { email = email.copy(value = it) },\n        errorMessage = email.error\n    )\n    Button(onClick = { if (validator.validate()) submit() }) {\n        Text(\"Submit\")\n    }\n}\n</code></pre> <pre><code>// Live validation as the user types\nLaunchedEffect(email.value) { validator.validate() }\n\n// Gate submit\nButton(\n    enabled = validator.valid,\n    onClick = { validator.validate(); submit() }\n) { Text(\"Submit\") }\n</code></pre>","path":["FormValidator"],"tags":[]},{"location":"error_safe/","level":1,"title":"ErrorSafe Pattern","text":"<p><code>errorSafe</code> bundles a field's current value, its original value, and its error message into one <code>remember</code>-able unit — eliminating three separate state variables per field.</p>","path":["ErrorSafe Pattern"],"tags":[]},{"location":"error_safe/#the-errorsafevaluet-data-class","level":2,"title":"The <code>ErrorSafeValue&lt;T&gt;</code> data class","text":"Property Type Description <code>value</code> <code>T</code> Current field value <code>initial</code> <code>T</code> Value at creation time <code>error</code> <code>String?</code> Current error message, or <code>null</code> <code>modified</code> <code>Boolean</code> <code>true</code> when <code>value != initial</code>","path":["ErrorSafe Pattern"],"tags":[]},{"location":"error_safe/#declaring-state","level":2,"title":"Declaring state","text":"<pre><code>var firstName by remember { errorSafe(\"\") }\nvar email     by remember { errorSafe(existingUser?.email ?: \"\") }\n</code></pre> <p>The argument to <code>errorSafe</code> becomes both the initial value and the baseline for <code>modified</code>.</p>","path":["ErrorSafe Pattern"],"tags":[]},{"location":"error_safe/#reading-and-writing","level":2,"title":"Reading and writing","text":"<pre><code>// Read\nText(firstName.value)\n\n// Write — copy to preserve other fields\nfirstName = firstName.copy(value = newValue)\n\n// Clear error\nfirstName = firstName.copy(error = null)\n</code></pre>","path":["ErrorSafe Pattern"],"tags":[]},{"location":"error_safe/#wiring-to-a-text-field","level":2,"title":"Wiring to a text field","text":"<pre><code>AppTextField(\n    label = \"First Name\",\n    value = firstName.value,\n    onChange = { firstName = firstName.copy(value = it) },\n    errorMessage = firstName.error\n)\n</code></pre>","path":["ErrorSafe Pattern"],"tags":[]},{"location":"error_safe/#checking-dirty-state","level":2,"title":"Checking dirty state","text":"<p><code>modified</code> is <code>true</code> as soon as the user changes the value from its starting point:</p> <pre><code>val isDirty = firstName.modified || email.modified\n\nButton(enabled = isDirty, onClick = { ... }) {\n    Text(\"Save\")\n}\n</code></pre> <p>This is useful for preventing unnecessary API calls when nothing has changed, or for showing a discard-changes prompt.</p>","path":["ErrorSafe Pattern"],"tags":[]},{"location":"error_safe/#wiring-errors-from-the-validator","level":2,"title":"Wiring errors from the validator","text":"<p>The <code>onError</code> callback on each <code>ValidationField</code> receives the error string (or <code>null</code> on success). Copy it onto the matching state:</p> <pre><code>ValidationField(\n    value = email.value,\n    name = \"Email\",\n    type = FormValidator.Type.Email\n) { errorMessage -&gt;\n    email = email.copy(error = errorMessage)\n}\n</code></pre>","path":["ErrorSafe Pattern"],"tags":[]},{"location":"how_it_works/","level":1,"title":"How It Works","text":"","path":["How It Works"],"tags":[]},{"location":"how_it_works/#overview","level":2,"title":"Overview","text":"<pre><code>FormValidator(flow, fields)\n        │\n        ▼\n  validator.validate()\n        │\n        ├─ Flow.Down  → iterate fields top-to-bottom, stop on first error\n        ├─ Flow.Up    → iterate fields bottom-to-top, stop on first error\n        └─ Flow.Splash → validate all fields, collect all errors\n        │\n        ▼\n  ValidationField.valid()\n        │\n        ├─ Type.Required     → blank/null check\n        ├─ Type.Email        → regex check\n        ├─ Type.MustBeMoreThan / MustBeLessThan / MustBeInRange → numeric compare\n        ├─ Type.MustBeEqualTo → equality check\n        ├─ Type.Custom       → caller-supplied predicate\n        └─ Type.Optional     → always passes\n        │\n        ▼\n  onError(message?) called on each field\n        │\n        ▼\n  validator.valid (Compose state) updated\n</code></pre>","path":["How It Works"],"tags":[]},{"location":"how_it_works/#composition-local","level":2,"title":"Composition local","text":"<p><code>Form(validator) { ... }</code> calls <code>CompositionLocalProvider(LocalFormValidator provides validator)</code>. Any composable nested inside can read the current validator without prop-drilling:</p> <pre><code>val validator = LocalFormValidator.current\n</code></pre>","path":["How It Works"],"tags":[]},{"location":"how_it_works/#snackbar-variant","level":2,"title":"Snackbar variant","text":"<p><code>Form(validator, snackBarProperties) { ... }</code> shows a <code>ValidationSnackBar</code> automatically when <code>validate()</code> returns <code>false</code>. <code>SnackBarProperties</code> controls the message text, duration, and appearance.</p> <pre><code>Form(\n    validator = validator,\n    snackBarProperties = SnackBarProperties(visibleDuration = 4000)\n) { ... }\n</code></pre>","path":["How It Works"],"tags":[]},{"location":"how_it_works/#validate-return-value","level":2,"title":"<code>validate()</code> return value","text":"<p><code>validate()</code> returns <code>true</code> only when every field that was checked passed. Use it to gate submission:</p> <pre><code>Button(onClick = {\n    if (validator.validate()) {\n        submitForm()\n    }\n}) { Text(\"Submit\") }\n</code></pre>","path":["How It Works"],"tags":[]},{"location":"how_it_works/#validatorvalid-state","level":2,"title":"<code>validator.valid</code> state","text":"<p><code>validator.valid</code> is a Compose <code>State&lt;Boolean&gt;</code> that reflects the outcome of the last <code>validate()</code> call. It recomposes any composable that reads it:</p> <pre><code>Button(enabled = validator.valid, onClick = { submit() }) {\n    Text(\"Submit\")\n}\n</code></pre>","path":["How It Works"],"tags":[]},{"location":"how_it_works/#validatorerrormessage","level":2,"title":"<code>validator.errorMessage</code>","text":"<p>When <code>Flow.Down</code> or <code>Flow.Up</code> stops at the first failing field, <code>validator.errorMessage</code> holds that field's error string. Useful for displaying a top-level error summary:</p> <pre><code>if (!validator.valid) {\n    Text(validator.errorMessage ?: \"Please fix the errors above\", color = MaterialTheme.colors.error)\n}\n</code></pre>","path":["How It Works"],"tags":[]},{"location":"installation/","level":1,"title":"Installation","text":"<p>FormValidator is published via JitPack.</p>","path":["Installation"],"tags":[]},{"location":"installation/#step-1-add-the-jitpack-repository","level":2,"title":"Step 1 — Add the JitPack repository","text":"Kotlin Gradle (settings.gradle.kts)Groovy (settings.gradle) <pre><code>dependencyResolutionManagement {\n    repositories {\n        maven { url = uri(\"https://jitpack.io\") }\n    }\n}\n</code></pre> <pre><code>dependencyResolutionManagement {\n    repositories {\n        maven { url 'https://jitpack.io' }\n    }\n}\n</code></pre>","path":["Installation"],"tags":[]},{"location":"installation/#step-2-add-the-dependency","level":2,"title":"Step 2 — Add the dependency","text":"Kotlin GradleGroovy <pre><code>implementation(\"com.github.funyin:FormValidator:LIBRARY_VERSION\")\n</code></pre> <pre><code>implementation 'com.github.funyin:FormValidator:LIBRARY_VERSION'\n</code></pre> <p>Current version</p> <p>Replace <code>LIBRARY_VERSION</code> with the latest tag from the releases page.</p>","path":["Installation"],"tags":[]},{"location":"installation/#supported-targets","level":2,"title":"Supported targets","text":"Target Minimum Android API 21 JVM (desktop) JVM 8 iOS iosArm64, iosX64, iosSimulatorArm64","path":["Installation"],"tags":[]},{"location":"installation/#requirements","level":2,"title":"Requirements","text":"<ul> <li>Kotlin 2.0+</li> <li>Compose Multiplatform 1.7+</li> </ul>","path":["Installation"],"tags":[]},{"location":"quick_start/","level":1,"title":"Quick Start","text":"","path":["Quick Start"],"tags":[]},{"location":"quick_start/#1-declare-field-state-with-errorsafe","level":2,"title":"1. Declare field state with <code>errorSafe</code>","text":"<pre><code>var firstName by remember { errorSafe(\"\") }\nvar lastName  by remember { errorSafe(\"\") }\nvar email     by remember { errorSafe(\"\") }\n</code></pre>","path":["Quick Start"],"tags":[]},{"location":"quick_start/#2-build-a-formvalidator","level":2,"title":"2. Build a <code>FormValidator</code>","text":"<pre><code>val validator = FormValidator(\n    flow = FormValidator.Flow.Down,\n    fields = buildList {\n        add(ValidationField(\n            value = firstName.value,\n            name = \"First Name\",\n            type = FormValidator.Type.Required\n        ) { firstName = firstName.copy(error = it) })\n\n        add(ValidationField(\n            value = lastName.value,\n            name = \"Last Name\",\n            type = FormValidator.Type.Required\n        ) { lastName = lastName.copy(error = it) })\n\n        add(ValidationField(\n            value = email.value,\n            name = \"Email\",\n            type = FormValidator.Type.Email\n        ) { email = email.copy(error = it) })\n    }\n)\n</code></pre> <p>Tip</p> <p>Rebuild the validator inside <code>remember(firstName.value, lastName.value, email.value)</code> so the field values it captures stay current.</p>","path":["Quick Start"],"tags":[]},{"location":"quick_start/#3-wrap-your-ui-in-form","level":2,"title":"3. Wrap your UI in <code>Form</code>","text":"<pre><code>Form(validator) {\n    AppTextField(\n        label = \"First Name\",\n        value = firstName.value,\n        onChange = { firstName = firstName.copy(value = it) },\n        errorMessage = firstName.error\n    )\n    AppTextField(\n        label = \"Last Name\",\n        value = lastName.value,\n        onChange = { lastName = lastName.copy(value = it) },\n        errorMessage = lastName.error\n    )\n    AppTextField(\n        label = \"Email\",\n        value = email.value,\n        onChange = { email = email.copy(value = it) },\n        errorMessage = email.error\n    )\n    Button(onClick = { if (validator.validate()) submit() }) {\n        Text(\"Submit\")\n    }\n}\n</code></pre>","path":["Quick Start"],"tags":[]},{"location":"quick_start/#4-optionally-enable-live-validation","level":2,"title":"4. Optionally enable live validation","text":"<pre><code>LaunchedEffect(firstName.value, lastName.value, email.value) {\n    validator.validate()\n}\n</code></pre>","path":["Quick Start"],"tags":[]},{"location":"quick_start/#5-gate-the-submit-button-on-dirty-state","level":2,"title":"5. Gate the submit button on dirty state","text":"<pre><code>val isDirty = firstName.modified || lastName.modified || email.modified\n\nButton(\n    enabled = isDirty,\n    onClick = { if (validator.validate()) submit() }\n) { Text(\"Submit\") }\n</code></pre>","path":["Quick Start"],"tags":[]},{"location":"roadmap/","level":1,"title":"Roadmap","text":"","path":["Roadmap"],"tags":[]},{"location":"roadmap/#done","level":2,"title":"Done","text":"<ul> <li><code>Required</code>, <code>Email</code>, <code>MustBeMoreThan</code>, <code>MustBeLessThan</code>, <code>MustBeInRange</code>, <code>MustBeEqualTo</code>, <code>Custom</code>, <code>Optional</code> validation types</li> <li><code>Flow.Down</code>, <code>Flow.Up</code>, <code>Flow.Splash</code> validation ordering</li> <li><code>ErrorSafeValue</code> with <code>modified</code> dirty tracking</li> <li><code>errorSafe()</code> convenience state builder</li> <li><code>Form</code> composable with <code>LocalFormValidator</code> composition local</li> <li>Snackbar integration via <code>SnackBarProperties</code></li> <li>Android, JVM (desktop), and iOS targets</li> <li>Maven Central publishing via NMCP</li> </ul>","path":["Roadmap"],"tags":[]},{"location":"roadmap/#planned","level":2,"title":"Planned","text":"<ul> <li>Cross-field validation rules (a field that depends on another field's current value without re-creating the validator)</li> <li>Compose Multiplatform Web target</li> <li>Accessibility: surface errors to semantics / TalkBack automatically</li> <li>Compose preview-friendly <code>LocalFormValidator</code> stub</li> </ul>","path":["Roadmap"],"tags":[]},{"location":"validation_types/","level":1,"title":"Validation Types","text":"<p>All validation rules are members of <code>FormValidator.Type&lt;T&gt;</code>, a sealed class.</p>","path":["Validation Types"],"tags":[]},{"location":"validation_types/#required","level":2,"title":"Required","text":"<p>Fails when the value is blank or null.</p> <pre><code>ValidationField(value = username.value, name = \"Username\", type = FormValidator.Type.Required) {\n    username = username.copy(error = it)\n}\n</code></pre>","path":["Validation Types"],"tags":[]},{"location":"validation_types/#email","level":2,"title":"Email","text":"<p>Checks that the value matches a valid email address format.</p> <pre><code>ValidationField(value = email.value, name = \"Email\", type = FormValidator.Type.Email) {\n    email = email.copy(error = it)\n}\n</code></pre>","path":["Validation Types"],"tags":[]},{"location":"validation_types/#mustbemorethan","level":2,"title":"MustBeMoreThan","text":"<p>Fails when a numeric value is not greater than <code>min</code>.</p> <pre><code>ValidationField(\n    value = age.value,\n    name = \"Age\",\n    type = FormValidator.Type.MustBeMoreThan(min = 18)\n) { age = age.copy(error = it) }\n</code></pre>","path":["Validation Types"],"tags":[]},{"location":"validation_types/#mustbelessthan","level":2,"title":"MustBeLessThan","text":"<p>Fails when a numeric value is not less than <code>max</code>.</p> <pre><code>ValidationField(\n    value = discount.value,\n    name = \"Discount\",\n    type = FormValidator.Type.MustBeLessThan(max = 100)\n) { discount = discount.copy(error = it) }\n</code></pre>","path":["Validation Types"],"tags":[]},{"location":"validation_types/#mustbeinrange","level":2,"title":"MustBeInRange","text":"<p>Fails when a numeric value falls outside <code>[min, max]</code>.</p> <pre><code>ValidationField(\n    value = quantity.value,\n    name = \"Quantity\",\n    type = FormValidator.Type.MustBeInRange(min = 1, max = 99)\n) { quantity = quantity.copy(error = it) }\n</code></pre>","path":["Validation Types"],"tags":[]},{"location":"validation_types/#mustbeequalto","level":2,"title":"MustBeEqualTo","text":"<p>Fails when the value does not equal <code>target</code>. Useful for confirm-password fields.</p> <pre><code>ValidationField(\n    value = confirmPassword.value,\n    name = \"Confirm Password\",\n    type = FormValidator.Type.MustBeEqualTo(target = password.value)\n) { confirmPassword = confirmPassword.copy(error = it) }\n</code></pre>","path":["Validation Types"],"tags":[]},{"location":"validation_types/#custom","level":2,"title":"Custom","text":"<p>Provide your own predicate. Return <code>null</code> to pass, or an error message string to fail.</p> <pre><code>ValidationField(\n    value = username.value,\n    name = \"Username\",\n    type = FormValidator.Type.Custom { value -&gt;\n        if (value.length &lt; 3) \"Username must be at least 3 characters\" else null\n    }\n) { username = username.copy(error = it) }\n</code></pre>","path":["Validation Types"],"tags":[]},{"location":"validation_types/#optional","level":2,"title":"Optional","text":"<p>Skips validation entirely — the field is always considered valid. Use for fields that should participate in <code>Flow.Down</code> ordering without blocking submission.</p> <pre><code>ValidationField(\n    value = bio.value,\n    name = \"Bio\",\n    type = FormValidator.Type.Optional\n) { bio = bio.copy(error = it) }\n</code></pre>","path":["Validation Types"],"tags":[]},{"location":"validation_types/#validation-flow","level":2,"title":"Validation flow","text":"<p><code>FormValidator.Flow</code> controls which fields are checked and in what order:</p> Flow Behaviour <code>Down</code> Top-to-bottom; stops at the first failing field <code>Up</code> Bottom-to-top; stops at the first failing field <code>Splash</code> All fields validated simultaneously <pre><code>FormValidator(flow = FormValidator.Flow.Splash, fields = ...)\n</code></pre>","path":["Validation Types"],"tags":[]},{"location":"community/","level":1,"title":"Community","text":"","path":["Community"],"tags":[]},{"location":"community/#get-help","level":2,"title":"Get help","text":"<ul> <li>GitHub Issues — bug reports and feature requests</li> <li>GitHub Discussions — questions and general conversation</li> </ul>","path":["Community"],"tags":[]},{"location":"community/#stay-up-to-date","level":2,"title":"Stay up to date","text":"<p>Watch the releases page to be notified when new versions ship.</p>","path":["Community"],"tags":[]},{"location":"community/#contribute","level":2,"title":"Contribute","text":"<p>See the Contributing guide for how to set up the project locally and open a pull request.</p>","path":["Community"],"tags":[]},{"location":"community/contributing/","level":1,"title":"Contributing","text":"","path":["Community","Contributing"],"tags":[]},{"location":"community/contributing/#prerequisites","level":2,"title":"Prerequisites","text":"<ul> <li>JDK 17+</li> <li>Android SDK (API 35)</li> <li>Kotlin 2.0+</li> </ul>","path":["Community","Contributing"],"tags":[]},{"location":"community/contributing/#local-setup","level":2,"title":"Local setup","text":"<pre><code>git clone https://github.com/funyin/FormValidator.git\ncd FormValidator\n\n# Build the library\n./gradlew :FormValidator:build\n\n# Run JVM unit tests\n./gradlew :FormValidator:test\n\n# Run Android instrumented tests (requires a connected device or emulator)\n./gradlew connectedAndroidTest\n</code></pre>","path":["Community","Contributing"],"tags":[]},{"location":"community/contributing/#running-the-demo-app","level":2,"title":"Running the demo app","text":"<p>Open the project in Android Studio and run the <code>app</code> configuration on any API 21+ device or emulator.</p>","path":["Community","Contributing"],"tags":[]},{"location":"community/contributing/#docs-setup","level":2,"title":"Docs setup","text":"<pre><code>brew install python@3.12        # one-time\npython3.12 -m venv venv         # one-time per clone\nsource venv/bin/activate        # every new terminal\npip install -r requirements.txt\nzensical serve\n</code></pre> <p>The local preview is at <code>http://127.0.0.1:8000</code>.</p>","path":["Community","Contributing"],"tags":[]},{"location":"community/contributing/#opening-a-pull-request","level":2,"title":"Opening a pull request","text":"<ol> <li>Fork the repository and create a branch from <code>master</code>.</li> <li>Make your changes and add or update tests where applicable.</li> <li>Run <code>./gradlew :FormValidator:test</code> to confirm nothing is broken.</li> <li>Open a pull request against <code>master</code>. Describe what changed and why.</li> </ol>","path":["Community","Contributing"],"tags":[]},{"location":"community/contributing/#reporting-a-bug","level":2,"title":"Reporting a bug","text":"<p>Open a GitHub Issue and include:</p> <ul> <li>FormValidator version</li> <li>Target platform (Android / JVM / iOS)</li> <li>A minimal reproducible example</li> <li>Expected vs actual behaviour</li> </ul>","path":["Community","Contributing"],"tags":[]},{"location":"examples/","level":1,"title":"Examples","text":"","path":["Examples"],"tags":[]},{"location":"examples/#validating-a-login-form","level":2,"title":"Validating a login form","text":"<pre><code>@Composable\nfun LoginForm(onLogin: (email: String, password: String) -&gt; Unit) {\n    var email    by remember { errorSafe(\"\") }\n    var password by remember { errorSafe(\"\") }\n\n    val validator = remember(email.value, password.value) {\n        FormValidator(\n            flow = FormValidator.Flow.Down,\n            fields = listOf(\n                ValidationField(email.value, \"Email\", FormValidator.Type.Email) {\n                    email = email.copy(error = it)\n                },\n                ValidationField(password.value, \"Password\", FormValidator.Type.Required) {\n                    password = password.copy(error = it)\n                }\n            )\n        )\n    }\n\n    Form(validator) {\n        OutlinedTextField(\n            value = email.value,\n            onValueChange = { email = email.copy(value = it) },\n            label = { Text(\"Email\") },\n            isError = email.error != null,\n            supportingText = { email.error?.let { Text(it) } }\n        )\n        OutlinedTextField(\n            value = password.value,\n            onValueChange = { password = password.copy(value = it) },\n            label = { Text(\"Password\") },\n            visualTransformation = PasswordVisualTransformation(),\n            isError = password.error != null,\n            supportingText = { password.error?.let { Text(it) } }\n        )\n        Button(onClick = {\n            if (validator.validate()) onLogin(email.value, password.value)\n        }) { Text(\"Log in\") }\n    }\n}\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#matching-a-confirm-password-field","level":2,"title":"Matching a confirm-password field","text":"<pre><code>var password        by remember { errorSafe(\"\") }\nvar confirmPassword by remember { errorSafe(\"\") }\n\nval validator = remember(password.value, confirmPassword.value) {\n    FormValidator(\n        flow = FormValidator.Flow.Down,\n        fields = listOf(\n            ValidationField(password.value, \"Password\", FormValidator.Type.Required) {\n                password = password.copy(error = it)\n            },\n            ValidationField(\n                value = confirmPassword.value,\n                name = \"Confirm Password\",\n                type = FormValidator.Type.MustBeEqualTo(target = password.value)\n            ) { confirmPassword = confirmPassword.copy(error = it) }\n        )\n    )\n}\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#validating-a-numeric-range","level":2,"title":"Validating a numeric range","text":"<pre><code>var seats by remember { errorSafe(1) }\n\nval validator = remember(seats.value) {\n    FormValidator(\n        flow = FormValidator.Flow.Splash,\n        fields = listOf(\n            ValidationField(\n                value = seats.value,\n                name = \"Seats\",\n                type = FormValidator.Type.MustBeInRange(min = 1, max = 10)\n            ) { seats = seats.copy(error = it) }\n        )\n    )\n}\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#using-a-custom-rule","level":2,"title":"Using a custom rule","text":"<pre><code>var username by remember { errorSafe(\"\") }\n\nval validator = remember(username.value) {\n    FormValidator(\n        flow = FormValidator.Flow.Down,\n        fields = listOf(\n            ValidationField(\n                value = username.value,\n                name = \"Username\",\n                type = FormValidator.Type.Custom { value -&gt;\n                    when {\n                        value.length &lt; 3  -&gt; \"At least 3 characters required\"\n                        value.contains(\" \") -&gt; \"Spaces are not allowed\"\n                        else -&gt; null\n                    }\n                }\n            ) { username = username.copy(error = it) }\n        )\n    )\n}\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#showing-a-snackbar-on-validation-failure","level":2,"title":"Showing a snackbar on validation failure","text":"<pre><code>Form(\n    validator = validator,\n    snackBarProperties = SnackBarProperties(visibleDuration = 3000)\n) {\n    // fields ...\n    Button(onClick = { validator.validate() }) { Text(\"Submit\") }\n}\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#validating-all-fields-simultaneously-with-splash-flow","level":2,"title":"Validating all fields simultaneously with Splash flow","text":"<p><code>Flow.Splash</code> reports errors on every failing field at once instead of stopping at the first one:</p> <pre><code>val validator = FormValidator(\n    flow = FormValidator.Flow.Splash,\n    fields = listOf(firstNameField, lastNameField, emailField)\n)\n</code></pre>","path":["Examples"],"tags":[]},{"location":"examples/#gating-submit-on-dirty-state","level":2,"title":"Gating submit on dirty state","text":"<pre><code>var name  by remember { errorSafe(profile.name) }\nvar email by remember { errorSafe(profile.email) }\n\nval hasChanges = name.modified || email.modified\n\nButton(\n    enabled = hasChanges,\n    onClick = { if (validator.validate()) save() }\n) { Text(\"Save changes\") }\n</code></pre>","path":["Examples"],"tags":[]}]}